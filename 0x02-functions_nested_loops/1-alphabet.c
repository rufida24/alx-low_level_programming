#include "main.h"

/**
 * main - check the code
 *
 * print_alphabet - utilizes the putchar function to print the alphabet a - z
 */

void print_alphabet(void)
{
	int ch;

	for (ch = 'a'; ch <= 'z'; ch++)
		_putchar(ch);
	_putchar('\n'
} 
